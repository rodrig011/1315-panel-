import { Card,CardContent } from "@/components/ui/card"; import { Progress } from "@/components/ui/progress"; import type { LucideIcon } from "lucide-react";
export function MetricCard({label,value,meta,percent,icon:Icon}:{label:string;value:string;meta?:string;percent?:number;icon:LucideIcon}){return <Card className="group transition duration-200 hover:-translate-y-0.5 hover:border-white/[.1] hover:bg-white/[.035]"><CardContent className="p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-[11px] font-medium text-muted-foreground">{label}</p><p className="mt-2 truncate text-[21px] font-semibold tracking-[-.035em]">{value}</p>{meta&&<p className="mt-1 truncate text-[10px] text-muted-foreground/80">{meta}</p>}</div><div className="grid size-8 shrink-0 place-items-center rounded-xl border border-white/[.055] bg-white/[.025] transition group-hover:bg-white/[.045]"><Icon className="size-3.5 text-muted-foreground"/></div></div>{percent!==undefined&&<Progress value={Math.min(percent,100)} className="mt-4 h-1"/>}</CardContent></Card>}
